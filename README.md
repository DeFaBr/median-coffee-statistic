# Основные моменты:
## 1 Хранение файлов:
Мне показалось более уместным, чтобы примеры и последующие csv файлы находились в одной папке "storage". Поэтому для использования новых файлов их сначала стоит положить в эту папку. 

Однако при запуске программы с ключом --files писать перед именами файлов "storage/" нет смысла, 
так как в дальнейшем в цикле к названию файла в начало добавляется "storage/" (файл "script.py" строка № 58)
```python
with open(("storage/"+file), 'r') as f:
```

## 2 Тесты:
### 2.1 Проверка на возможность привести к нужному типу данных:
Поле **имя** я не вижу смылса проверять так как возможных имён может быть много, а также если просто проверить на знаки или цифры, 
то в будущем при разделении людей с одинаковыми именами и фамилиями проще всего это можно будет сделать через добавление в конце цифры 
(['Иван Иванович 1', ...], ['Иван Иванович 2', ...]) , поэтому при таком случае тест начнёт валиться

Похожая ситуация может произойти и с полями **настроение** и **предмет**, только в данном случае список настроений или предметов может как увеличиться 
(также могут войти в обиход цифры и знаки), так и уменьшиться до ('хорошо', 'плохо') и только информатика соответственно

### 2.2 Остальные случаи:
Проверку значений на отрицательность и проверку расширений самих файлов я считаю здесь избыточной
